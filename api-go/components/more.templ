package components

import "strconv"
templ More(endpoint string, page int) {

	<div class="container">
		<button class="btn btn-primary" hx-post={endpoint + "?page=" + strconv.Itoa(page + 1)} hx-target="#search-results" hx-swap="innerHTML">Load More</button>
	</div>
}